import * as api from "../api/postAPI"
import * as types from "../constants/postConstants"
import { PostCreationData } from "../api/type"

export const addPostAction = (post: PostCreationData) => async (dispatch) => {
  try {
  } catch (error) {}
}
